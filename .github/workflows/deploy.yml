name: CI

on:
  push:
    branches: [ "main" ]
    paths: [ "crates/nobit/*" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Packages
      run: make pkgs

    - name: Build
      run: make build

    - name: Test
      run: make test
